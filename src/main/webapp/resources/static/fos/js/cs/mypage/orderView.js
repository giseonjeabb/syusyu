namespace("orderView");
orderView = {
    initLoad: () => {
        // 페이지 로드시 실행될 코드
    },

    bindButtonEvent: () => {
        // 이벤트 핸들러를 연결하는 코드
    },
}

namespace("orderView.eventHandler");
orderView.eventHandler = {
    // 특정 이벤트를 처리하는 함수들
}

namespace("orderView.function");
orderView.function = {
    // 페이지 내에서 사용되는 주요 함수들
}